user nginx;

worker_processes auto;

events {
    worker_connections 1024;
}

http {
    server {
        listen 8080;
        location /vendors {
            proxy_pass http://vendor-service:8080;
        }
    }
}